apiVersion: v1
kind: ConfigMap
metadata:
  name: house-service-config
  namespace: hotketok
data:
  application-prod.yml: |
    server:
      port: 8090
    
    spring:
      application:
        name: house-service
      
      datasource:
        url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:}?serverTimezone=Asia/Seoul
        username: ${DB_USERNAME}
        password: ${DB_PASSWORD}
        driver-class-name: com.mysql.cj.jdbc.Driver
      jpa:
        hibernate:
          ddl-auto: update
        properties:
          hibernate:
            format_sql: true
            dialect: org.hibernate.dialect.MySQL8Dialect
      
      cloud:
        kubernetes:
          discovery:
            enabled: true
            all-namespaces: true
          config:
            enabled: true
            sources:
              - namespace: hotketok
                name: house-service-config
    
    logging:
      level:
        org.hibernate.SQL: debug
        com.hotketok: info
    
    eureka:
      client:
        enabled: false
        
    client:
      user-service:
        url: http://user-service.hotketok.svc.cluster.local:8088
      infra-service:
        url: http://infra-service.hotketok.svc.cluster.local:8089