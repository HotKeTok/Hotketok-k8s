# hotketok-post-service MySQL

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-post-pvc
  namespace: hotketok
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: nks-block-storage

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-post-deploy
  namespace: hotketok
spec:
  selector:
    matchLabels:
      app: mysql-post
  replicas: 1
  template:
    metadata:
      labels:
        app: mysql-post
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: post-service-secret
                  key: DB_ROOT_PASSWORD
          envFrom:
            - secretRef:
                name: post-service-secret
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-post-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-post-storage
          persistentVolumeClaim:
            claimName: mysql-post-pvc
---

apiVersion: v1
kind: Service
metadata:
  name: mysql-post-service
  namespace: hotketok
spec:
  type: NodePort
  selector:
    app: mysql-post
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      nodePort: 32005