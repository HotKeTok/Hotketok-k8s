apiVersion: apps/v1
kind: Deployment

metadata:
  name: user-service-deploy
  namespace: hotketok

spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service

  template:
    metadata:
      labels:
        app: user-service
    spec:
      imagePullSecrets:
        - name: ncr-secret
      containers:
        - name: user-service
          image: hotketok-reg-real.kr.ncr.ntruss.com/hotketok/hotketok-user-service:latest
          ports:
            - containerPort: 8088
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: SPRING_CONFIG_LOCATION
              value: file:/config/
          envFrom:
            - secretRef:
                name: user-service-secret
          volumeMounts:
            - name: config-volume
              mountPath: /config
              readOnly: true
      volumes:
        - name: config-volume
          configMap:
            name: user-service-config