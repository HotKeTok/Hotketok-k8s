# hotketok-user-service MySQL

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-user-pvc
  namespace: hotketok
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: nks-block-storage

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-user-deploy
  namespace: hotketok
spec:
  selector:
    matchLabels:
      app: mysql-user
  replicas: 1
  template:
    metadata:
      labels:
        app: mysql-user
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: user-service-secret
                  key: DB_ROOT_PASSWORD
          envFrom:
            - secretRef:
                name: user-service-secret
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-user-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-user-storage
          persistentVolumeClaim:
            claimName: mysql-user-pvc
---

apiVersion: v1
kind: Service
metadata:
  name: mysql-user-service
  namespace: hotketok
spec:
  type: NodePort
  selector:
    app: mysql-user
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      nodePort: 32008