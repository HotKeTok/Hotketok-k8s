apiVersion: apps/v1
kind: Deployment

metadata:
  name: notice-service-deploy
  namespace: hotketok

spec:
  replicas: 1
  selector:
    matchLabels:
      app: notice-service

  template:
    metadata:
      labels:
        app: notice-service
    spec:
      imagePullSecrets:
        - name: ncr-secret
      containers:
        - name: notice-service
          image: contest57-registry-real.kr.ncr.ntruss.com/hotketok/hotketok-notice-service:latest
          ports:
            - containerPort: 8098
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: SPRING_CONFIG_LOCATION
              value: file:/config/
          envFrom:
            - secretRef:
                name: notice-service-secret
          volumeMounts:
            - name: config-volume
              mountPath: /config
              readOnly: true
      volumes:
        - name: config-volume
          configMap:
            name: notice-service-config