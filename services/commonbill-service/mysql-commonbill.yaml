# hotketok-commonbill-service MySQL

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-commonbill-pvc
  namespace: hotketok
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: nks-block-storage

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-commonbill-deploy
  namespace: hotketok
spec:
  selector:
    matchLabels:
      app: mysql-commonbill
  replicas: 1
  template:
    metadata:
      labels:
        app: mysql-commonbill
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: commonbill-service-secret
                  key: DB_ROOT_PASSWORD
          envFrom:
            - secretRef:
                name: commonbill-service-secret
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-commonbill-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-commonbill-storage
          persistentVolumeClaim:
            claimName: mysql-commonbill-pvc
---

apiVersion: v1
kind: Service
metadata:
  name: mysql-commonbill-service
  namespace: hotketok
spec:
  type: NodePort
  selector:
    app: mysql-commonbill
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      nodePort: 32002