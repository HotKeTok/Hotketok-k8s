# hotketok-estimate-service MySQL

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-estimate-pvc
  namespace: hotketok
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: nks-block-storage

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-estimate-deploy
  namespace: hotketok
spec:
  selector:
    matchLabels:
      app: mysql-estimate
  replicas: 1
  template:
    metadata:
      labels:
        app: mysql-estimate
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: estimate-service-secret
                  key: DB_ROOT_PASSWORD
          envFrom:
            - secretRef:
                name: estimate-service-secret
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-estimate-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-estimate-storage
          persistentVolumeClaim:
            claimName: mysql-estimate-pvc
---

apiVersion: v1
kind: Service
metadata:
  name: mysql-estimate-service
  namespace: hotketok
spec:
  type: NodePort
  selector:
    app: mysql-estimate
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      nodePort: 32003